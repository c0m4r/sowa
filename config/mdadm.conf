# /etc/mdadm.conf - Linux software RAID assembly policy
#
# Arrays are discovered from their on-device metadata.  Add stable ARRAY lines
# after creating an array with: mdadm --detail --scan >> /etc/mdadm.conf
DEVICE partitions containers
AUTO +all
