# ~/.bashrc - this account's interactive shell.
#
# useradd copies this file out of /etc/skel when it creates a home directory.
# The prompt and the colour aliases are not here: they live in /etc/bash.bashrc,
# which Bash reads before this file, so that changing them once changes them for
# everyone. What follows is yours, and overrides anything the system set.
