# ~/.bashrc - root's interactive shell.
#
# The prompt and the colour aliases are not here: they live in /etc/bash.bashrc,
# which Bash reads before this file and which every account on the system shares.
# What follows is root's alone, and overrides anything the system set.
